# ETL
pdfplumber>=0.10.3
camelot-py[cv]>=0.11.0
opencv-python-headless>=4.8.0
ghostscript>=0.7

# Data
pandas>=2.1.0
numpy>=1.26.0

# Torch CPU-only (must be installed BEFORE sentence-transformers to prevent
# pip from pulling the 900MB CUDA wheel)
# Install separately via: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch is listed here only so it appears in the lockfile; the separate
# install command below handles the correct index URL.

# Indexing
sentence-transformers>=2.7.0
# Hosted vector DB (Qdrant Cloud) — keeps the index outside the backend's
# ephemeral filesystem so the API stays stateless. See .env.example.
qdrant-client>=1.12.0
tiktoken>=0.7.0
# scipy is a transitive dep of scikit-learn (pulled in by sentence-transformers);
# pin it explicitly so a partial install doesn't break with ModuleNotFoundError.
scipy>=1.11.0

# Agent / LLM
# Pin to the 0.3.x line: newer langchain (>=0.4) removed AgentExecutor /
# create_react_agent from langchain.agents, which breaks our ReAct agent with
# "ImportError: cannot import name 'AgentExecutor'". Keep these in lockstep.
langchain>=0.3.7,<0.4.0
langchain-core>=0.3.21,<0.4.0
langchain-openai>=0.2.9,<0.3.0
langchain-community>=0.3.7,<0.4.0
langchain-text-splitters>=0.3.2,<0.4.0
openai>=1.30.0

# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
# Required by FastAPI to parse multipart/form-data (the /upload file endpoint).
# Not pulled in automatically by `fastapi`, so it must be listed explicitly.
python-multipart>=0.0.9
# Verifies the Supabase access token (HS256) on protected routes.
pyjwt>=2.8.0

# Logging
structlog>=24.1.0

# Utilities
python-dotenv>=1.0.0
